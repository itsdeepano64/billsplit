// This component is kept for backward compatibility.
// The main expense form is now handled inline in app/(app)/expenses/page.tsx
export default function AddExpenseButton({ onSaved }: { onSaved?: () => void }) {
  return null;
}
